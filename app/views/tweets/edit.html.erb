<div class="contents row">
    <h3>
      ブログを編集する
    </h3>
  <%= form_for "/tweets/#{@tweet.id}", method: "patch" do |f| %>
<%= f.text_field :name, placeholder: '名前を入力' %>
<%= f.text_field :text, placeholder: '本文を入力' %>
<%= f.submit value = "SENT" %>
  <% end %>
</div>
